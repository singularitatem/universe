load("@rules_rust//rust:defs.bzl", "rust_binary")

rust_binary(
    name = "server",
    srcs = ["src/main.rs"],
    compile_data = ["//ui:static/index.html"],
    deps = [
        "@crates//:axum",
        "@crates//:serde",
        "@crates//:tokio",
    ],
)
